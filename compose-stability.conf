// Compose compiler stability configuration.
// Classes matched here are treated as stable by the Compose compiler, letting
// composables that take them as parameters skip recomposition when unchanged.
// Cross-module classes (from :domain, :data, :player) are otherwise inferred
// unstable because those modules don't run the Compose compiler.
//
// Only list types that are effectively immutable (vals, immutable collections).
// Docs: https://developer.android.com/develop/ui/compose/performance/stability/fix#configuration-file

// Domain models — all immutable data classes / enums.
com.streamvault.domain.model.**

// java.time types used by EPG/guide UI — immutable by contract.
java.time.**
